<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fixers.coolspot">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ViewProfile2"
            android:label="@string/title_activity_view_profile2"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".AddItems2"
            android:label="@string/title_activity_add_items2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".TestView"
            android:label="@string/title_activity_test_view"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".HomeActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".WelcomeActivity" />
        <activity android:name=".Login" />
        <activity android:name=".SignUp" />
    </application>

</manifest>